// import { selectGettestimonialDomain } from '../selectors';

describe('selectGettestimonialDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
