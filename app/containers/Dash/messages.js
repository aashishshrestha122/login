/*
 * Dash Messages
 *
 * This contains all the text for the Dash container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Dash';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Dash container!',
  },
});
